# DXCore4 Analytics Dashboard

Интерактивный дашборд для анализа метрик разработки на основе данных из Jira.

## 🚀 GitHub Pages

Дашборд доступен онлайн: [https://github.com/leshchinskaya/dxcore-4-statistic](https://github.com/leshchinskaya/dxcore-4-statistic)

### Как использовать на GitHub Pages:

1. Откройте страницу дашборда
2. Нажмите кнопку **"📤 Загрузить данные"**
3. Выберите файл `issues_flat.json` с вашего компьютера
4. Дашборд автоматически обработает данные и отобразит метрики

## 📊 Возможности

- **Метрики производительности**: анализ эффективности команды
- **Трендовый анализ**: динамика изменения показателей во времени
- **Математические формулы**: подробное описание расчетов
- **Интерактивные фильтры**: по проектам, исполнителям, направлениям, спринтам, эпикам
- **Экспорт в Excel**: сохранение данных для дальнейшего анализа
- **Детальный анализ задач**: просмотр проблемных зон

## 🛠️ Локальная разработка

### Получение данных из Jira

```bash
# Настройте переменные окружения
export JIRA_URL="https://your-domain.atlassian.net"
export JIRA_USER="your-email@example.com"
export JIRA_TOKEN="your-api-token"

# Запустите скрипт экспорта
./jira_metrics.sh
```

Это создаст файл `issues_flat.json` с данными из Jira.

### Запуск дашборда локально

```bash
# Запустите локальный сервер
./start_dashboard.sh

# Откройте в браузере
# http://localhost:8080
```

## 📁 Структура проекта

- `index.html` - главная страница дашборда
- `dashboard.js` - основная логика и обработка данных
- `dashboard.css` - стили интерфейса
- `analytics.js` - дополнительная аналитика
- `jira_metrics.sh` - скрипт экспорта данных из Jira
- `issues_flat.json` - файл с данными (не включен в репозиторий)

## 🔐 Безопасность

⚠️ **Важно**: Файл `issues_flat.json` содержит конфиденциальные данные и не должен загружаться в публичный репозиторий.

- Файл уже добавлен в `.gitignore`
- При использовании GitHub Pages данные загружаются только в браузер пользователя
- Данные не отправляются на сервер и остаются локальными

## 📈 Метрики

Дашборд рассчитывает следующие метрики:

- **Производительность**: (Оценка / Затраченное время) × 100%
- **Среднее время выполнения**: от Todo до Done
- **Процент отладки**: доля времени на исправление багов
- **Процент коммуникаций**: доля времени на сервисные задачи
- **Change Requests**: количество доработок и изменений

Подробнее о формулах расчета - во вкладке "🧮 Математика" дашборда.

## 🤝 Вклад в проект

Если вы хотите улучшить дашборд, создайте Pull Request или откройте Issue.

## 📄 Лицензия

Этот проект разработан для внутреннего использования.
